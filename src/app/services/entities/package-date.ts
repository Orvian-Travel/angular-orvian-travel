export class PackageDate {
    constructor(
        public id: string,
        public startDate: string,
        public endDate: string,
        public qtd_available: number,
        public travelPackageId: string
    ) {

    }
}
